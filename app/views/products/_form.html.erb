<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :productname %>
    <%= form.text_field :productname, id: :product_productname %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :product_price %>
  </div>

  <div class="field">
    <%= form.label :seller_id %>
    <%= form.text_field :seller_id, id: :product_seller_id %>
  </div>

  <div class="field">
    <%= form.label :sellerpercentage %>
    <%= form.text_field :sellerpercentage, id: :product_sellerpercentage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
